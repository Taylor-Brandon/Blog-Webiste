<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Blog</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div id="nav-bar">
  <nav>
        <a id="home" href="/">Home</a>
        {{#if loggedIn}}
        <a id="dash" href="/dashboard/:id">Dashboard</a>
        <a id="logout" href="/logout">Logout</a>
        {{else}}
         <a id="login" href="/login">Login</a>
         <a id="signup" href="/signup">Sign Up</a>
         {{/if}}
        <header>
      <h1>
        Star Wars Blog
      </h1>
    </header>
    </nav>
  </div>
    
    
      <main id="body" class="container container-fluid mt-5">
        {{{body}}}
      </main>
      {{#if loggedIn}}
  <script src="../../public/js/logout.js"></script>
  {{else}}
  <script type="application/json" src="../../public/js/login.js"></script>
  <script type="application/json" src="../../public/js/signup.js"></script>
  {{/if}}
  </body>
</html>